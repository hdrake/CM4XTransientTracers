# Analysis scripts and notebooks

These scripts and jupyter notebooks should be run in order, based on the numbered prefixes
(e.g. starting with `0_*`), as later numbered scripts will assume the existence of
postprocessed data produced by earlier code.
